## Part A

**Q1.** Under a truthful transition manifest, the gate refused all 240 transition looks, labeling them UNKNOWN before any inference was made.

**Q2.** At the first 100k look, falsely declaring the same transition data stationary produced 80/80 wrong-side decisions (all 80 drift records were wrong for both false-declaration methods).

**Q3.** "Tail truth" labels the stationary regime that persists forever after the finite parameter change (from the existing exact two-class capacity method). It is explicitly not the finite-window queue state: "Eventual-tail stability is not finite-window queue state."

**Q4.** No. The note states: "The declaration gate cannot detect a lying or missing manifest from data."

**Q5.** At the 320k look, the service_improve scenario still fails under the false declaration: the scheduled method is 20/20 wrong, while the anytime method outputs 20/20 UNKNOWN (not wrong, but not correct either).

**Q6.** Not stated. The note gives "two parent models and seeds 9301–9310" and reports "120 workloads," but it never states or explains how 120 workloads decomposes into independent replications, nor explicitly claims the 120 are independent.

**Q7.** From "existing exact two-class capacity," which labels the stationary regime that persists forever after the finite change — i.e., an existing analytic/exact method, not something derived from the observed data. The Evidence boundary also notes "post-regime exact labels ... were recomputed."

**Q8.** (a) New time-varying stability theorem — not claimed (explicitly listed under "What this research does not show"). (b) Validity on real GPU clusters — not claimed ("nonexponential/real-GPU transfer" listed as not shown). (c) Established novelty — not claimed (explicitly listed under "does not show"). (d) Independent external replication — not claimed; explicitly stated as zero ("External independent replications: zero").

**Q9.** The reproduction command recomputes 360 recorded looks and regenerates 24 small looks.

## Part B

Skip (per instructions).

## Part C

Only file accessed: `<SCRATCH>/readers/BASE-2.prompt.md`

No URLs were accessed; no other files or tools were used.
